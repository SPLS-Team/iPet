# iPet does not read API keys from this file in normal use.
# Configure the LLM key in the application Settings tab.
#
# Optional development defaults can still be documented here:
IPET_DEFAULT_BASE_URL=https://api.openai.com/v1
IPET_DEFAULT_MODEL=gpt-4.1-mini

