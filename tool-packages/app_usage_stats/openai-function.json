{
  "type": "function",
  "function": {
    "name": "app_usage_stats",
    "description": "统计本机各应用的累计前台使用时长。可按 today/7d/30d 范围查询，返回按应用聚合的秒数和按日趋势。仅本机存储。",
    "parameters": {
      "type": "object",
      "properties": {
        "range": {
          "type": "string",
          "enum": ["today", "7d", "30d"],
          "default": "today",
          "description": "统计范围：今天 / 近 7 天 / 近 30 天。"
        },
        "limit": {
          "type": "integer",
          "minimum": 1,
          "maximum": 100,
          "default": 15,
          "description": "最多返回多少个应用（按总时长降序）。"
        }
      },
      "additionalProperties": false
    }
  }
}
